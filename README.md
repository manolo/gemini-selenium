# gemini-selenium

This is a `gemini` plugin that starts `selenium` before running tests.

## Installation
`npm install gemini-selenium`

## Configuration

```yml
system:
    plugins:
      selenium: true
```
